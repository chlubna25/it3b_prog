#include <stdio.h>

int main() {
    int cislo;
    printf("Zadejte kladne cislo: ");
    scanf("%d", &cislo);

    printf("Delitele cisla %d jsou:\n", cislo);
    for (int i = 1; i <= cislo; i++) {
        if (cislo % i == 0) {
            printf("%d\n", i);
        }
    }

    return 0;
}